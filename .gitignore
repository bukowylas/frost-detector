# Raw NOAA ISD data -- large (~350 MB) and fully reproducible via fetch_data.py.
# The whole point of the pipeline is that anyone can regenerate it.
data_raw
/home/marci/data/frost-detector/

# Large processed data (regenerated by prepare.py). Keep metrics.json (results).
data/nights.csv
# Fitted model artifact (regenerated by `predict.py --fit`).
data/model.joblib

# Internal assistant working-notes (not project documentation).
CLAUDE.md

# Python
__pycache__/
*.pyc
.ruff_cache/

# Local cloud-cover archive (the prior project's superseded code; kept locally
# for history, not part of the public Frost Detector repo).
_cloud_cover_archive/
